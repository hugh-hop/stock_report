export const GAME_CONFIG = {
  canvasWidth: 640,
  canvasHeight: 360,
  pixelScale: 2,
  gravity: 0.5,
  groundY: 280,
  maxHp: 100,
  normalAttackDamage: 8,
  heavyAttackDamage: 18,
  defenseReduction: 0.6,
  moveSpeed: 2.5,
  jumpForce: -7,
  roundTime: 99,
  hurtStunFrames: 20,
  attackFrames: 15,
  heavyAttackFrames: 25,
  hitCooldownFrames: 5,
  attackRange: 28,
  heavyAttackRange: 35,
  knockbackForce: 4,
  heavyKnockbackForce: 7,
  energyGainOnHit: 15,
  energyGainOnBlock: 5,
  heavyAttackEnergyCost: 30,
  maxEnergy: 100,
}

export function createMecha(x, facing, color, accentColor, name) {
  return {
    x,
    y: GAME_CONFIG.groundY - 48,
    width: 32,
    height: 48,
    velocityX: 0,
    velocityY: 0,
    hp: GAME_CONFIG.maxHp,
    maxHp: GAME_CONFIG.maxHp,
    energy: 50,
    maxEnergy: GAME_CONFIG.maxEnergy,
    facing,
    state: 'idle',
    stateTimer: 0,
    attackType: 'normal',
    isGrounded: true,
    comboCount: 0,
    animFrame: 0,
    animTimer: 0,
    hitCooldown: 0,
    color,
    accentColor,
    name,
  }
}
